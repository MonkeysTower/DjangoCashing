[project]
name = "project"
version = "0.1.0"
description = ""
authors = [
    {name = "Andrey Dostovalov",email = "Trifandre@yandex.ru"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django (>=5.2,<6.0)",
    "starlette (>=0.46.2,<0.47.0)"
]

[tool.poetry]
packages = [{include = "project", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
